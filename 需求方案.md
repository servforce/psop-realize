# 四个 Markdown 生成需求文档

## 1. 目标

标准库界面选中一个国家标准 PDF 后，系统调用 `qwen3.7-plus` 生成四个 Markdown 文件：

- `standard_body.md`
- `standard_structure.md`
- `standard_logic.md`
- `standard_overview.md`

这四个文件用于把 PDF 标准整理成可阅读、可理解、可检索的材料。

其中 `standard_body.md` 是核心文件，先由 PDF 生成；其余三个文件基于 `standard_body.md` 继续生成。

## 2. 固定决策

本阶段固定以下决策：

- 只使用 `qwen3.7-plus`。
- 不配置备用模型。
- 不做“失败后切换其他模型”。
- 不要求程序自己深度解析 PDF 内容。
- PDF 内容理解、正文整理、结构归纳、逻辑归纳、检索概要生成，都交给 `qwen3.7-plus` 完成。
- 程序主要负责上传 PDF、组织 prompt、调用模型、保存四个 Markdown 文件。
- 用户提供的参考 `.md` 只作为效果风格参考，不作为程序硬编码模板。

## 3. 总体生成顺序

生成顺序固定为：

```text
原始 PDF
→ qwen3.7-plus 生成 standard_body.md
→ qwen3.7-plus 基于 standard_body.md 生成 standard_structure.md
→ qwen3.7-plus 基于 standard_body.md 生成 standard_logic.md
→ qwen3.7-plus 基于 standard_body.md 生成 standard_overview.md
```

说明：

- `standard_body.md` 是后续三个文件的主要依据。
- `standard_structure.md`、`standard_logic.md`、`standard_overview.md` 原则上不再直接读取原始 PDF。
- 如果后续实际接入时发现 `qwen3.7-plus` API 不能直接读取 PDF，可以由程序先做 PDF 输入适配，例如提取文本、上传文件、转图片或使用平台文档解析能力，但业务逻辑仍然是由 `qwen3.7-plus` 生成 Markdown。

## 4. 四个文件职责

### 4.1 standard_body.md

`standard_body.md` 是 PDF 标准正文整理稿。

它应该尽量完整、干净地呈现 PDF 中的标准内容，包括首页基本信息、前言、正文、表格、术语、注释、附录等。

它不是摘要，也不是解读。

### 4.2 standard_structure.md

`standard_structure.md` 是标准内容架构文件。

它从 `standard_body.md` 中归纳章节结构、层级关系和主要章节内容。

### 4.3 standard_logic.md

`standard_logic.md` 是章节内部逻辑关系文件。

它基于 `standard_body.md` 归纳标准的章节关系、前后依赖、流程逻辑、风险控制逻辑等。

### 4.4 standard_overview.md

`standard_overview.md` 是标准检索概要文件。

后续根据视频分析结果匹配最适合的国家标准 PDF 时，主要使用 `standard_overview.md`。

它需要面向“视频分析结果”描述这个标准适合什么视频内容、不适合什么视频内容、什么情况下应返回该标准。

## 5. Markdown 顶部 YAML front matter

四个 Markdown 文件都保留少量 YAML front matter。

YAML front matter 只用于系统识别文件角色和标准身份，不承载大段正文内容。

统一字段：

```yaml
---
standard_name: "标准中文名称"
standard_number: "标准编号"
source_pdf: "源 PDF 文件名"
document_role: "文件角色"
---
```

`document_role` 取值：

```text
standard_body
standard_structure
standard_logic
standard_overview
```

`standard_overview.md` 额外增加：

```yaml
retrieval_input_type: "video_analysis_result"
```

## 6. standard_body.md 生成要求

`standard_body.md` 的具体正文格式由 `qwen3.7-plus` 根据 PDF 内容决定，不在程序中硬编码。

prompt 只需要约束以下方向：

- 生成完整的标准正文整理稿。
- 保留 PDF 首页中的标准基本信息。
- 保留前言和正文内容。
- 保留章节编号、条款编号、术语编号、表格标题、表格内容、注释、来源说明。
- 表格尽量整理为 Markdown 表格。
- 删除明显的页眉、页脚、目录点线、孤立页码等 PDF 排版噪声。
- 不需要标注内容来自 PDF 第几页。
- 不要把正文改写成摘要。
- 不要加入模型自己的解释、建议或扩展内容。
- 如果某处内容无法识别，可以在 Markdown 中直接标注需要核对原文。

用户提供的参考 `.md` 可以作为 `standard_body.md` 的效果参考：整体风格应干净、完整、可读，像人工整理后的标准正文 Markdown。

## 7. standard_structure.md 生成要求

`standard_structure.md` 基于 `standard_body.md` 生成。

prompt 只需要要求模型：

- 提取标准的章节结构。
- 展示章节层级。
- 简要说明每个章节主要承载什么内容。
- 不要引入 `standard_body.md` 中不存在的章节。
- 不需要 PDF 页码。

具体呈现方式由模型决定，可以使用章节树、表格或两者结合。

## 8. standard_logic.md 生成要求

`standard_logic.md` 基于 `standard_body.md` 生成。

prompt 只需要要求模型：

- 说明章节之间的逻辑关系。
- 说明前置条件、作业流程、风险控制、验证要求之间的关系。
- 归纳标准内部的流程逻辑和依赖关系。
- 不要编造标准正文中不存在的内容。

具体呈现方式由模型决定。

## 9. standard_overview.md 生成要求

`standard_overview.md` 基于 `standard_body.md` 生成。

它是后续标准检索使用的核心文件。

prompt 需要要求模型生成以下内容：

- 标准基本信息。
- 标准适用的视频内容画像。
- 标准不适用的视频内容画像。
- 匹配决策规则。

匹配决策规则需要明确区分：

- 应返回。
- 可作为候选返回。
- 不应返回。

注意：

- 不写典型用户问题。
- 不写 PDF 页码。
- 不写详细章节目录。
- 面向视频分析结果匹配，而不是面向普通用户问答。
- 不使用“可以作为参考但不作为主要依据”这类模糊表达。

## 10. qwen3.7-plus 调用方式

每个 Markdown 文件使用一次独立模型调用生成。

不建议一次调用同时生成四个 Markdown。

建议调用顺序：

```text
call 1: PDF → standard_body.md
call 2: standard_body.md → standard_structure.md
call 3: standard_body.md → standard_logic.md
call 4: standard_body.md → standard_overview.md
```

模型参数建议：

```text
model: qwen3.7-plus
temperature: 0.1 或 0.2
top_p: 0.8
```

每次调用要求：

- 只输出目标 Markdown 文件内容。
- 不输出解释说明。
- 不用代码块包裹 Markdown。
- 不额外输出“以下是生成结果”等话术。

## 11. 程序侧职责

程序侧只做以下事情：

- 接收用户选择的 PDF。
- 调用 `qwen3.7-plus`。
- 按顺序生成四个 Markdown。
- 保存四个 Markdown 文件。
- 做基础校验。
- 在界面中展示生成结果或下载入口。

程序侧不需要硬编码正文排版细节。

基础校验包括：

- 四个 Markdown 是否生成。
- 文件内容是否为空。
- YAML front matter 是否存在。
- `document_role` 是否正确。
- `standard_body.md` 是否包含标准名称和正文标题。
- `standard_overview.md` 是否包含“应返回 / 可作为候选返回 / 不应返回”。

## 12. 本阶段结论

本阶段实现时，按以下原则执行：

```text
只用 qwen3.7-plus。
先生成 standard_body.md。
用户参考 .md 只作为风格参考，不作为固定模板。
不要在程序里硬编码 body 的复杂排版规则。
body 的正文整理由模型根据 PDF 自行完成。
其余三个 Markdown 基于 body 生成。
overview 是后续标准检索唯一核心文件。
```
