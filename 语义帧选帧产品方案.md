# 语义帧选帧产品方案

## 1. 背景

当前“语义帧”页已经能按文本段落展示：

- 该段落内的候选业务帧
- 按相似度排序后的语义匹配结果
- 最终可进入线框图生成的业务帧预览

上一个版本里，文本段落切得更细以后，语义帧抽取明显更准了。现在真正需要补齐的不是“怎么再多找一些候选”，而是“最终到底选哪几张作为业务帧”，让它和后续要转成线框图的帧数量保持一致，并且规则足够稳定、可解释。

## 2. 目标

这一步的目标可以压成一句话：

> 从每个文本段落的候选业务帧里，选出最能代表该段落的少量图片，最终总数与线框图目标数量一致。

具体目标有四个：

- 保证每个关键文本段落都有代表帧
- 尽量避免同一画面附近的重复入选
- 最终入选数量和线框图数量对齐
- 规则要简单，方便人工理解和后续调参

## 3. 推荐规则

### 3.1 基本原则

先按段落选，再全局收口。

也就是说，不建议直接从所有候选帧里做“全局相似度前 N 张”。
更稳的做法是先保证“每个段落至少有一个代表”，再根据整体名额做最后裁剪。

### 3.2 默认选法

每个文本段落默认先取 **top1** 作为主业务帧。

这样做的好处是：

- 简单
- 结果稳定
- 和“一个小步骤对应一张关键画面”的业务直觉一致

### 3.3 何时允许取 top2

当某个段落出现下面情况时，可以允许补一张备选帧：

- 该段落本身包含多个动作或状态变化
- top1 和 top2 的相似度接近，差距不明显
- top1 只能表达一半意思，top2 补足另一个画面角度

这里的核心不是“固定每段取 2 张”，而是“只有在确实需要补表达时才扩展到 2 张”。

### 3.4 最终收口规则

最终入选业务帧总数，应该和线框图目标数量保持一致。

如果前面按段落选出来的数量超过目标数：

- 优先保留相似度更高的帧
- 同时尽量保证不同段落都有覆盖
- 不让同一段落的多张相近图片挤占全部名额

如果前面按段落选出来的数量少于目标数：

- 先补那些相似度高、表达完整的候选帧
- 再考虑同段落的第二候选
- 不为了凑数引入低质量或重复感很强的图片

## 4. 建议的决策口径

如果现在要给产品和研发一个统一口径，我建议定成下面这版：

1. **默认策略**：每个文本段落选 1 张主业务帧。
2. **扩展策略**：只有当段落存在明显多动作、多状态或相似度接近时，才允许补 1 张。
3. **全局策略**：最终选帧数量必须和线框图数量一致。
4. **裁剪策略**：超出名额时，优先保留高相似度且段落覆盖更均衡的帧。

## 5. 不建议的方案

### 5.1 不建议全局直接取相似度前 N

原因很简单：

- 容易把同一镜头附近的图片都选进去
- 容易遗漏某些段落的代表帧
- 结果会偏“分数高”，但不一定偏“业务像”

### 5.2 不建议每段固定取 3 张或更多

原因是：

- 语义段已经切细了，继续固定取很多张会回到“候选过多”的问题
- 后续线框图数量有限，太多备选只会增加筛选负担

## 6. 产品层面的结论

这次的产品方向不是“多选”，而是“更准地少选”。

最佳方案是：

- 先按段落保证覆盖
- 再按相似度和表达完整度决定是否补图
- 最后统一收口到线框图数量

这样得到的结果会更像真正的业务帧，而不是单纯的相似度排行榜。

