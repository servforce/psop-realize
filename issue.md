# 背景 + 目标 + 计划 + 怎么算完成

# 视频

## 完整转写分段（未做）

需求：本地ASR需要有每句话的起始时间json文件
目前：判断筛选关键帧需先分段，所以暂且用qwen模型来随机估计的时间（不准确）

## 解析关键帧

目前：本地做的。。。
初步想法：（这样做的话 就不需要把关键帧列表和筛选后的关键帧分开列出来了）
          1. 先转写结构化文本段落
          2. 在每个段落时间框内做密集抽帧（2s一帧）
          3. 对候选帧做 质量筛选 和 vl模型判断
          4. 每个段落保留多少帧 + 全局保留多少帧


## 筛选关键帧
需求：关键帧是否有业务意义
 1. 传统图像算法 ->  只能判断图像画面质量
 2. vl大模型 -> 图像业务意义
 3. 混合方案：
    1. 现有逻辑生成候选关键帧
    2. 本地基础质量过滤
      - **清晰度**
         - OpenCV + Laplacian 方差
           - 1. 读入图片；
             1. 转灰度图；
             2. 对灰度图做 Laplacian 边缘响应；
             3. 计算方差；
             4. 方差越低，说明边缘越少，越可能模糊。
      - **亮度/曝光**
      - **黑屏/过曝/严重模糊**
        - OpenCV 灰度均值 + 亮度分布比例
          - 1. 图片转灰度；
            1. 计算平均亮度；
            2. 统计接近黑色的像素比例；
            3. 统计接近白色的像素比例。
    3. 按时间顺序或场景分组
      - 每组 8 张候选帧
    4. 调用 Qwen3-VL-Flash 或 Qwen3-VL-Plus
      - 判断每张帧是否适合生成线框图
      - 输出重要性分数
      - 输出入选原因
      - 输出是否存在关键动作/状态变化/文字变化/接线变化
    5. 业务规则收口
      - 每个分段至少保留 1 张
      - 每个分段最多保留 24 张
      - 风险、检查、接线、状态变化优先
      - 模型不确定时保守保留
    6. 线框图任务只处理 selected_for_wireframe=true 的帧

# 标准库

# 标准库检索

向量检索：Postgresql + pgvector

  1. 读取 standard_overview.md
  2. 生成 search_content
  3. 调用 embedding 模型
  4. 写入 PostgreSQL 向量索引
  5. 用户查询时做 pgvector 检索
  6. 返回相关标准


读取 standard_overview.md
    -> 清洗成 search_content
    -> 调 embedding 模型
    -> 写 standard_search_indexes
    -> 查询时用 pgvector 匹配

embedding 模型：负责 文本 -> 向量
pgvector：负责 存向量 + 比较向量相似度 + 找最近的向量
PostgreSQL：负责 存业务表 + 执行 SQL 查询

# 调用记录

