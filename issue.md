############################
issue:
  1. 不写总的issue，（每一块分成不同的issue）
      某观点：越长文档发给codex越混乱，反而是拆分出很多小文档，每个小文档讲清楚局部逻辑，效率更高。
  2. 每个小功能拆成不同issue，issue间逻辑关系写一个什么文件

  在gitlab里有三类标签：
    1. 业务标签： [功能 + bug +修复 + 讨论]
    2. 优先级标签： 高、中、低
    3. 纵向业务维度：视频，标准库等都各自是一个纵向维度
  基于上面三个维度生成一些标签出来，给issue打上一些标签


视频部分：
  1. 转写文本分成句子：每个段落有几个句子，每个句子可以得到一个embedding，句子embedding和图片语义embedding算距离。
  2. 

  原始关键帧 -> 抽取语义帧前 可以先进行去重：色彩去重 + 结构去重（减少对大模型和对外接口的调用次数）
      - 阶段一：筛选出来的语义帧不要有肢体
  抽取语义帧：拿原始关键帧的图像语义向量和句子的文本语义向量去做余弦相似度比较（最简版本）
             vl模型直接可以做图像语义embedding
      --> 不好的地方再去优化

关键帧：
  1. 解析关键帧 FFmpeg
  2. 去重
  3. 抽取语义帧（进一步过滤筛选）：
      拿原始关键帧的图像语义向量和句子的文本语义向量去做余弦相似度比较（最简版本）
      或者
      每个图片抽取语义描述，再去跟字幕比对
      或者


标准库部分：
  脚本自动爬取pdf标准：工程化的东西
    有一些skill可以规避反爬
      1. 先找一些skill，明确任务目标，让他帮你制定好一个实现的方式  
          先要明确（爬什么网站，存什么数据，怎么周期调度，数据后面怎么处理，错误日志怎么处理）
      
标准库检索部分：
  返回前三个分数最高的标准pdf

############################


# 项目背景

项目名称：Servforce Material Workbench
项目路径：https://github.com/servforce/Octopus

该项目主要用于素材和标准数据的管理与检索。目前页面左侧包含以下四个功能模块：
  - 视频
  - 标准库
  - 标准库检索
  - 调用记录

本 Issue 主要用于记录各模块的功能内容、当前开发情况以及后续需要完善的事项。

# 视频

## 计划功能
  1. 上传视频，展示进度条
  2. 展示视频列表：可进行关键词搜索 + 按照上传时间排序
  3. 视频分析可实现：转写文本 + 解析关键帧 + 生成线框图 + 生成最终markdown
      - 转写文本：调用本地ASR服务 + qwen3.7-plus
      - 解析关键帧：FFmpeg
      - 生成线框图：qwen-image-2.0-pro
      - 生成markdown：树结构
## 目标
    关键在于最终的markdown中：每一段文本段落对应的关键帧是否准确。

## 问题
  1. 本地ASR服务需返回每个句子对应的起始时间（json文件）。

## 待办初步想法
  视频分析部分：
    1. 先做转写文本，.txt内容是：
        - 一级标题（视频标题）
        - 二级标题（文本段落名称）
            文本段落起始时间
        - 三级标题：正文
            文本段落具体内容
    **2. 解析关键帧：（解析中筛选）**
        - 每个段落起始时间内密集抽帧 2s
        - 对该段落内候选帧做cv质量筛选 + vl模型做视觉判断
        - 每个段落内保留 1-3 张最有代表性的操作帧
      
      需测试成本：
        qwen3-vl-plus / Flash 模型测成本（视频长度和成本基本正比）
         
    3. 生成线框图：
        步骤2 筛选出的关键帧全部转为线框图
    4. 生成markdown：
        根据1、2、3内容总结成树结构


# 标准库

## 计划功能
  1. 上传标准，展示进度条
  2. 展示标准列表：可进行关键词搜索 + 按照上传时间排序
  3. 解析标准结果：standard_overview.md + standard_structure.md + standard_logic.md + standard_body.md
      - qwen3.7-plus先将.pdf解析为 standard_body.md
      - qwen3.7-plus根据 standard_body.md 分别解析出 standard_overview.md + standard_structure.md + standard_logic.md

## 目标
    1、向量化standard_overview.md是否可准确的根据输入检索到此标准

## 问题
  暂无

## 待办初步想法
  脚本自动爬取国家标准pdf，反爬程序？



# 标准库检索

## 计划功能
  1. postgresql + pgvector实现向量化检索

## 目标
  检索准确

## 问题
  检索准确性需测试，score参数设置待测试

## 待办初步想法
  目前：
  1. 建索引：向量化每个标准的 standard_overview.md 对应数据库的一行索引记录 text-embedding-v4
  2. 检索：pgvector相似度匹配

  目前实现结果不准确，可能是score参数设置不合理:
  测试：构造50条测试集 [query,应命中的标准pdf的ID]
        取threshold=最高F1分数


# 调用记录
。。。。。

































## 完整转写分段（未做）

需求：本地ASR需要有每句话的起始时间json文件
目前：判断筛选关键帧需先分段，所以暂且用qwen模型来随机估计的时间（不准确）

## 解析关键帧

目前：本地做的。。。
初步想法：（这样做的话 就不需要把关键帧列表和筛选后的关键帧分开列出来了）
          1. 先转写结构化文本段落
          2. 在每个段落时间框内做密集抽帧（2s一帧）
          3. 对候选帧做 质量筛选 和 vl模型判断
          4. 每个段落保留多少帧 + 全局保留多少帧


## 筛选关键帧
需求：关键帧是否有业务意义
 1. 传统图像算法 ->  只能判断图像画面质量
 2. vl大模型 -> 图像业务意义
 3. 混合方案：
    1. 现有逻辑生成候选关键帧
    2. 本地基础质量过滤
      - **清晰度**
         - OpenCV + Laplacian 方差
           - 1. 读入图片；
             1. 转灰度图；
             2. 对灰度图做 Laplacian 边缘响应；
             3. 计算方差；
             4. 方差越低，说明边缘越少，越可能模糊。
      - **亮度/曝光**
      - **黑屏/过曝/严重模糊**
        - OpenCV 灰度均值 + 亮度分布比例
          - 1. 图片转灰度；
            1. 计算平均亮度；
            2. 统计接近黑色的像素比例；
            3. 统计接近白色的像素比例。
    3. 按时间顺序或场景分组
      - 每组 8 张候选帧
    4. 调用 Qwen3-VL-Flash 或 Qwen3-VL-Plus
      - 判断每张帧是否适合生成线框图
      - 输出重要性分数
      - 输出入选原因
      - 输出是否存在关键动作/状态变化/文字变化/接线变化
    5. 业务规则收口
      - 每个分段至少保留 1 张
      - 每个分段最多保留 24 张
      - 风险、检查、接线、状态变化优先
      - 模型不确定时保守保留
    6. 线框图任务只处理 selected_for_wireframe=true 的帧

# 标准库

# 标准库检索

向量检索：Postgresql + pgvector

  1. 读取 standard_overview.md
  2. 生成 search_content
  3. 调用 embedding 模型
  4. 写入 PostgreSQL 向量索引
  5. 用户查询时做 pgvector 检索
  6. 返回相关标准


读取 standard_overview.md
    -> 清洗成 search_content
    -> 调 embedding 模型
    -> 写 standard_search_indexes
    -> 查询时用 pgvector 匹配

embedding 模型：负责 文本 -> 向量
pgvector：负责 存向量 + 比较向量相似度 + 找最近的向量
PostgreSQL：负责 存业务表 + 执行 SQL 查询

# 调用记录

