# 项目背景

项目名称：Octopus Video Service
项目路径：https://github.com/servforce/Octopus

该项目用于视频素材的解析与管理。标准库、标准库检索相关内容已迁出到 `D:\work\psop_standard`。

本 Issue 用于记录视频模块的功能内容、当前开发情况以及后续需要完善的事项。

# Issue 管理约定

1. 不写总的 issue，每一块分成不同的 issue。
    某观点：越长文档发给 codex 越混乱，反而是拆分出很多小文档，每个小文档讲清楚局部逻辑，效率更高。
2. 每个小功能拆成不同 issue，issue 间逻辑关系单独写一个文件。

GitLab 中有三类标签：
  1. 业务标签：功能 + bug + 修复 + 讨论
  2. 优先级标签：高、中、低
  3. 纵向业务维度：视频等各自是一个纵向维度

基于上面三个维度生成标签，给 issue 打上标签。

# 视频

## 关键帧解析方案

### 方案1
YOLOv8m-world 目标检测 -> MobileSAM mask

问题：机械臂相关组件识别不稳定，bbox 质量受限，影响后续 mask 效果。

### 方案2
TinySAM 全图自动出 mask -> 过滤/去重 -> 每个 mask 生成 bbox -> 裁 crop -> 微调后的 YOLOv8s-world 做类别识别 -> 类别回填到 mask -> 建图索引

## 计划功能
  1. 上传视频，展示进度条
  2. 展示视频列表：可进行关键词搜索 + 按照上传时间排序
  3. 视频分析可实现：转写文本 + 解析关键帧 + 生成线框图 + 生成最终markdown
      - 转写文本：调用本地ASR服务 + qwen3.7-plus
      - 解析关键帧：FFmpeg
      - 生成线框图：qwen-image-2.0-pro
      - 生成markdown：树结构

## 目标
    关键在于最终的markdown中：每一段文本段落对应的关键帧是否准确。

## 问题
  1. 本地ASR服务需返回每个句子对应的起始时间（json文件）。

## 待办初步想法
  视频分析部分：
    1. 先做转写文本，.txt内容是：
        - 一级标题（视频标题）
        - 二级标题（文本段落名称）
            文本段落起始时间
        - 三级标题：正文
            文本段落具体内容
    **2. 解析关键帧：（解析中筛选）**
        - 每个段落起始时间内密集抽帧 2s
        - 对该段落内候选帧做cv质量筛选 + vl模型做视觉判断
        - 每个段落内保留 1-3 张最有代表性的操作帧

      需测试成本：
        qwen3-vl-plus / Flash 模型测成本（视频长度和成本基本正比）

    3. 生成线框图：
        步骤2 筛选出的关键帧全部转为线框图
    4. 生成markdown：
        根据1、2、3内容总结成树结构

## 语义帧抽取

  1. 转写文本分成句子：每个段落有几个句子，每个句子可以得到一个 embedding，句子 embedding 和图片语义 embedding 算距离。
  2. 原始关键帧 -> 抽取语义帧前先进行去重：色彩去重 + 结构去重（减少对大模型和对外接口的调用次数）
      - 阶段一：筛选出来的语义帧不要有肢体
  3. 抽取语义帧：拿原始关键帧的图像语义向量和句子的文本语义向量做余弦相似度比较（最简版本）
      - vl 模型直接可以做图像语义 embedding
      - 不好的地方再去优化

  关键帧流程：
  1. 解析关键帧 FFmpeg
  2. 去重
  3. 抽取语义帧（进一步过滤筛选）：
      - 拿原始关键帧的图像语义向量和句子的文本语义向量做余弦相似度比较（最简版本）
      - 或者每个图片抽取语义描述，再去跟字幕比对

## 完整转写分段（未做）

需求：本地ASR需要有每句话的起始时间json文件
目前：判断筛选关键帧需先分段，所以暂且用qwen模型来随机估计的时间（不准确）

## 解析关键帧

初步想法：（这样做的话 就不需要把关键帧列表和筛选后的关键帧分开列出来了）
          1. 先转写结构化文本段落
          2. 在每个段落时间框内做密集抽帧（2s一帧）
          3. 对候选帧做 质量筛选 和 vl模型判断
          4. 每个段落保留多少帧 + 全局保留多少帧

## 筛选关键帧
需求：关键帧是否有业务意义
 1. 传统图像算法 ->  只能判断图像画面质量
 2. vl大模型 -> 图像业务意义
 3. 混合方案：
    1. 现有逻辑生成候选关键帧
    2. 本地基础质量过滤
      - **清晰度**
         - OpenCV + Laplacian 方差
           1. 读入图片；
           2. 转灰度图；
           3. 对灰度图做 Laplacian 边缘响应；
           4. 计算方差；
           5. 方差越低，说明边缘越少，越可能模糊。
      - **亮度/曝光**
      - **黑屏/过曝/严重模糊**
        - OpenCV 灰度均值 + 亮度分布比例
          1. 图片转灰度；
          2. 计算平均亮度；
          3. 统计接近黑色的像素比例；
          4. 统计接近白色的像素比例。
    3. 按时间顺序或场景分组
      - 每组 8 张候选帧
    4. 调用 Qwen3-VL-Flash 或 Qwen3-VL-Plus
      - 判断每张帧是否适合生成线框图
      - 输出重要性分数
      - 输出入选原因
      - 输出是否存在关键动作/状态变化/文字变化/接线变化
    5. 业务规则收口
      - 每个分段至少保留 1 张
      - 每个分段最多保留 24 张
      - 风险、检查、接线、状态变化优先
      - 模型不确定时保守保留
    6. 线框图任务只处理 selected_for_wireframe=true 的帧
