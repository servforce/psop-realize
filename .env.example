APP_ENV=dev
# PostgreSQL is recommended for the first shared version.
# For a single-machine smoke test, change to sqlite:///./data/workbench.db.
DATABASE_URL=postgresql+psycopg://mcp_writer:change-me@10.0.0.20:5432/servforce_mcp
# DATABASE_URL=postgresql+psycopg://mcp_writer:密码@数据库IP:5432/servforce_mcp

# Use local storage by default so the demo can run without MinIO.
STORAGE_BACKEND=local
LOCAL_STORAGE_ROOT=./storage

# Set STORAGE_BACKEND=minio to use MinIO/S3-compatible object storage.
OBJECT_STORE_ENDPOINT=http://10.0.0.20:9000
OBJECT_STORE_ACCESS_KEY=minioadmin
OBJECT_STORE_SECRET_KEY=minioadmin
OBJECT_STORE_BUCKET=servforce-materials
OBJECT_STORE_STANDARD_BUCKET=servforce-standards
OBJECT_STORE_REGION=us-east-1
OBJECT_STORE_SECURE=false

# Caller identity used by local MCP Server and API smoke calls.
MCP_CLIENT_ID=local-dev
MCP_USER=
MCP_API_KEY=

# Optional existing tools/services.
WIREFRAME_TOOL_DIR=./tools/qwen-image-wireframe
VIDEO_WIREFRAME_GENERATION_ENABLED=false
WIREFRAME_IMAGE_MODEL=qwen-image-2.0-pro
WIREFRAME_IMAGE_SIZE=auto
WIREFRAME_TIMEOUT_SECONDS=900

# Qwen text model used by the standard library. Video Markdown is generated from transcript_tree.json.
QWEN_TEXT_API_KEY=
QWEN_TEXT_MODEL=qwen3.7-plus
# Video transcript semantic structuring model used by the 转写文本 step, not by 生成 Markdown.
TRANSCRIPT_STRUCTURE_MODEL=qwen3.7-plus
# Qwen VL model used to select which candidate video frames should generate wireframes.
# Leave QWEN_VL_API_KEY empty to reuse QWEN_TEXT_API_KEY or DASHSCOPE_API_KEY.
QWEN_VL_API_KEY=
QWEN_VL_MODEL=qwen3-vl-plus
QWEN_VL_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
QWEN_VL_TEMPERATURE=0
QWEN_VL_TOP_P=0.8
QWEN_VL_MAX_TOKENS=6000
QWEN_VL_TIMEOUT_SECONDS=300
QWEN_VL_MAX_INPUT_CHARS=30000
VIDEO_FRAME_SELECTION_ENABLED=true
VIDEO_FRAME_SELECTION_GROUP_SIZE=8
VIDEO_FRAME_SELECTION_MAX_SELECTED_FRAMES=24
VIDEO_FRAME_SELECTION_IMAGE_MAX_SIDE=768
VIDEO_FRAME_SELECTION_JPEG_QUALITY=85
VIDEO_FRAME_SELECTION_MIN_BLUR_SCORE=25
VIDEO_FRAME_SELECTION_DARK_RATIO_THRESHOLD=0.9
VIDEO_FRAME_SELECTION_BRIGHT_RATIO_THRESHOLD=0.9
VIDEO_FRAME_SELECTION_MIN_MEAN_BRIGHTNESS=8
VIDEO_FRAME_SELECTION_MAX_MEAN_BRIGHTNESS=248
# Standard library search model. Not used by the video Markdown step.
STANDARD_EMBEDDING_API_KEY=
STANDARD_EMBEDDING_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
STANDARD_EMBEDDING_MODEL=text-embedding-v4
STANDARD_EMBEDDING_DIMENSIONS=1024
STANDARD_EMBEDDING_TIMEOUT_SECONDS=120
STANDARD_VECTOR_SEARCH_MIN_SCORE=0
QWEN_TEXT_TEMPERATURE=0.1
QWEN_TEXT_TOP_P=0.8
QWEN_TEXT_MAX_TOKENS=24000
QWEN_TEXT_TIMEOUT_SECONDS=900
QWEN_TEXT_MAX_INPUT_CHARS=180000
QWEN_TEXT_FILE_UPLOAD_PURPOSE=file-extract

LOCAL_ASR_API_BASE_URL=http://10.0.0.20:12302
LOCAL_ASR_LANGUAGE=zh
LOCAL_ASR_TIMEOUT_SECONDS=3600
LOCAL_ASR_MAX_RETRIES=1
LOCAL_ASR_MODEL_LABEL=Qwen/Qwen3-ASR-1.7B
VIDEO_WORKDIR=./work/videos
VIDEO_MAX_UPLOAD_BYTES=2147483648
VIDEO_MAX_ANALYZED_FRAMES=10
STANDARD_WORKDIR=./work/standards
WORKER_POLL_INTERVAL_SECONDS=1

# Qwen image model key used by the wireframe tool.
DASHSCOPE_API_KEY=
