# 关键帧筛选功能使用说明

## 功能概述

关键帧筛选工具用于快速测试和验证图像语义分割效果。它使用 YOLO-E 26n-seg 模型对上传的图片进行物体检测和语义分割。

## 使用步骤

1. **输入操作描述**
   - 在文本框中输入操作场景的描述
   - 例如: "使用螺丝刀将舵机固定到底座上"
   - 系统会自动提取关键词并转换为英文

2. **上传图片**
   - 点击"选择图片"按钮,或直接拖拽图片到上传区域
   - 支持同时上传多张图片
   - 支持的格式: JPG, JPEG, PNG

3. **开始分割**
   - 点击"开始分割"按钮
   - 系统会处理所有上传的图片
   - 处理完成后会显示结果

## 结果展示

每张图片会显示:
- 分割后的可视化图像(带彩色边界框和标注)
- 检测到的对象列表
- 每个对象的置信度

## 关键词提取逻辑

系统会:
1. 从文本中提取关键词(如"螺丝刀"、"舵机"、"底座")
2. 应用同义词映射,转换为英文(如"screwdriver"、"servo"、"base")
3. 自动添加"hand"关键词(如果文本中没有提及手部)
4. 使用这些英文关键词作为YOLO模型的检测目标

## 注意事项

- 第一次加载模型可能需要较长时间
- 图片数量较多时,处理时间会相应增加
- 本地运行使用CUDA加速(如果可用)
